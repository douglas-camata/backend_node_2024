function calcularAreaTriagulo(altura, largura){
    const area = (altura * largura) / 2
    return area
}

let valorArea = calcularAreaTriagulo(5, 10)
console.log(`A área do triangulo é: ${valorArea}`)

console.log(`A área do triangulo é: ${calcularAreaTriagulo(3, 5)}`) 